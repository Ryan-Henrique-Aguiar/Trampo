spring.application.name=backend
spring.datasource.url=jdbc:postgresql://localhost:5432/trampo
spring.datasource.username=postgres
spring.datasource.password=1234
spring.datasource.driver-class-name=org.postgresql.Driver
db.url=jdbc:postgresql://localhost:5432/trampo
db.User=postgres
db.password=1234
api.security.token.secret=${JWT_SECRET:ambiente-dev-ryan}
api.security.cors-origin=${CORS_ORIGIN:http://localhost:4200}
file.upload-dir=./uploads
# Tamanho maximo de um unico arquivo
spring.servlet.multipart.max-file-size=10MB
# Tamanho maximo do conjunto de arquivos enviados na mesma requisicao
spring.servlet.multipart.max-request-size=50MB
